# Vercel Deployment Environment Variables
# Copy these to your Vercel project settings

# Application
APP_NAME=OpenFlow
VERSION=0.1.0
ENVIRONMENT=production
DEBUG=false
SERVERLESS=true

# Security (CRITICAL - Generate a strong secret key!)
SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY_AT_LEAST_32_CHARS_LONG
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database (Use Vercel Postgres, Neon, or Supabase with connection pooling)
# Format: postgresql+asyncpg://user:password@host:port/database
# For Vercel Postgres: Use the connection string from Vercel dashboard
# For Neon: Use pooled connection (port 5432) from Neon dashboard
# For Supabase: Use the connection pooling URL (port 6543)
DATABASE_URL=postgresql+asyncpg://user:password@host.region.neon.tech:5432/dbname?sslmode=require
DATABASE_ECHO=false
DATABASE_POOL_SIZE=1
DATABASE_MAX_OVERFLOW=0
DATABASE_POOL_RECYCLE=3600
DATABASE_POOL_PRE_PING=true

# Redis (Use Upstash Redis or Redis Cloud)
# Get this from: https://upstash.com/ or https://redis.com/
REDIS_URL=rediss://default:password@redis-host.upstash.io:6379

# CORS (Add your Vercel deployment domains)
# Format: ["https://your-app.vercel.app","https://your-custom-domain.com"]
CORS_ORIGINS=["https://your-app.vercel.app","https://your-app-preview.vercel.app"]
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=["*"]
CORS_ALLOW_HEADERS=["*"]

# File uploads (Vercel has 4.5MB payload limit)
MAX_UPLOAD_SIZE=4500000
ALLOWED_FILE_EXTENSIONS=[".pdf",".doc",".docx",".xls",".xlsx",".jpg",".jpeg",".png",".gif",".svg"]

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Server (Not used in Vercel, but kept for local testing)
HOST=0.0.0.0
PORT=8000
WORKERS=1

# NOTES:
# 1. Do NOT commit .env files to git - they contain secrets!
# 2. Set these variables in Vercel Dashboard: Project Settings > Environment Variables
# 3. Use different values for Production, Preview, and Development environments
# 4. For DATABASE_URL, use a connection pooler (PgBouncer, Neon, or Supabase)
# 5. For REDIS_URL, use a serverless Redis (Upstash recommended)
# 6. Generate SECRET_KEY: python -c "import secrets; print(secrets.token_urlsafe(32))"
