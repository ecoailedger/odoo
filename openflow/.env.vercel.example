# Vercel Testing Environment Variables (INSECURE - FOR TESTING ONLY!)
# ⚠️  WARNING: This configuration has NO SECURITY - implement authentication before production!

# ============================================================================
# MINIMAL SETUP FOR TESTING (Only these 3 variables are required)
# ============================================================================

# Application Mode
ENVIRONMENT=development
DEBUG=true
SERVERLESS=true

# Database (Use Vercel Postgres or Neon - FREE TIER AVAILABLE)
# For Vercel Postgres: Get from Vercel Dashboard > Storage > Create Database
# For Neon: Get from https://neon.tech (use pooled connection string)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# Redis (Use Upstash Redis - FREE TIER AVAILABLE)
# Get from: https://console.upstash.com/redis
REDIS_URL=rediss://default:password@redis-host.upstash.io:6379

# ============================================================================
# OPTIONAL - Only set these if you need to customize
# ============================================================================

# CORS (defaults allow all Vercel domains)
# CORS_ORIGINS=["*"]

# Database pooling (defaults are optimized for serverless)
# DATABASE_POOL_SIZE=1
# DATABASE_MAX_OVERFLOW=0

# Logging
# LOG_LEVEL=INFO

# ============================================================================
# QUICK START GUIDE
# ============================================================================
#
# 1. Get a FREE database:
#    Option A - Vercel Postgres:
#      - Go to vercel.com/dashboard
#      - Click your project > Storage > Create Database > Postgres
#      - Copy the DATABASE_URL
#
#    Option B - Neon (Recommended):
#      - Go to https://neon.tech
#      - Create account and project
#      - Copy the "Pooled connection" string
#      - Use port 5432 (not 5433)
#
# 2. Get FREE Redis:
#    - Go to https://console.upstash.com/redis
#    - Create database
#    - Copy the connection URL (starts with rediss://)
#
# 3. Set in Vercel:
#    - Go to vercel.com > Your Project > Settings > Environment Variables
#    - Add the 3 required variables above (SERVERLESS, DATABASE_URL, REDIS_URL)
#    - Deploy!
#
# 4. Test your deployment:
#    curl https://your-app.vercel.app/health
#    curl https://your-app.vercel.app/web
#
# ============================================================================
# NOTES
# ============================================================================
#
# - Security is DISABLED for easy testing
# - All endpoints are PUBLIC (no authentication required)
# - DO NOT use this configuration for production!
# - Add authentication before exposing to real users
# - Free tiers: Neon (512MB), Upstash (10K commands/day), Vercel (Hobby)
